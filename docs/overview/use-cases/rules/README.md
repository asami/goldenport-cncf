# Rule Use Case Overview

`overview.svg` is the primary comparison of current Rule use cases.

Theme-specific detail:

- `application-rule-evaluation.svg`: available explicit evaluation.
- `lifecycle-rule-constraint.svg`: partial automatic Constraint integration.
- `monitored-condition-event.svg`: partial reactive Production/Event
  integration.

The use-case status reflects the complete path, not only the availability of the
Rule Engine family used by that path.

# Phase 59.10 DOC-10-C1 Current-Document Contradiction Audit

status = historical, evidence-bearing, non-normative
phase = Phase 59.10
slice = P5910-DOC10-C1

## Purpose and boundary

This record completes the current-document contradiction audit after Core
Steps A and B. It records the authority distinction used by the Phase 59.10
checklist; it is not a new product or specification contract. The Phase
59.10 checklist remains `status=planned` and DOC-10 remains `IN_PROGRESS`.

This Slice does not begin Phase 60 and does not perform the final Phase release
closure. No current latest requirement is left only in an implementation note,
journal, phase checklist, or test. Historical notes and journals retain
history only; they do not supply a competing current contract.

## Audit result

The current Core design and specification are:

- `docs/design/component-documentation-knowledge-package.md`;
- `docs/spec/component-documentation-knowledge-package.md`.

The implementation note
`docs/notes/component-documentation-knowledge-package-implementation.md` is
historical and superseded. The referenced resource, knowledge, Help, and
framework documentation retains its stated authority. No stale claim in those
historical records is treated as current.

The cross-repository authority result is:

- The current public SimpleModeling.org projection is
  `cncf.framework-publication.v1`. Its static publication-contract script
  `scripts/test/check-cncf-framework-publication-contract.sh` passed in Step B.
- ai-directive's mounted `core/` and selected profile are authoritative. The
  public development guide is descriptive and cannot override the mounted
  authority. Its static public-Directive projection script
  `scripts/test/check-cncf-public-directive-projection.sh` passed in Step B.
- Textus CBD Support is the primary detail, usage, and review surface based on
  declared carrier metadata. Textus BoK provides the complementary semantic
  and exact-existence handoff to CBD Support. Neither replaces Core or Phase 58
  resolver authority.

These findings preserve the existing Core resource, knowledge, Help, and
framework documentation boundaries. They do not promote an implementation
detail, journal entry, phase record, or executable test into a current
authority source.

## Evidence state

Core Step A is committed as
`7c996b12eabf972aa040f2a86d7c02c49afb9a38`. Cross-repository Step B is
committed as `cd493b7c2c5618732e784d0600b46eff85e3dcd3`. Step B records that
the SimpleModeling.org and ai-directive static scripts named above passed.
The current-document audit commit and final Phase release closure remain for
the parent workflow.

## Explicit non-goals

This Slice performs no schema, API, resolver, carrier, endpoint, runtime, or
`SkillBundleManifest` installation/activation work. It does not begin Phase
60, change Core/Phase 58 authority, or modify any external repository.

## Dashboard discrepancy deferred to final release

`docs/phase/README.md` still calls 59.10 next/not-active, and the Phase 59
child map is still planned. These are release-close bookkeeping discrepancies,
not competing product or specification contracts. Their correction is deferred
to the final Phase release closure. Those files are intentionally not modified
in this Slice.

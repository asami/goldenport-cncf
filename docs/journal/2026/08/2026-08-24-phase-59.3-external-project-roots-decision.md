# Phase 59.3 External Project Roots Decision

decision_id=D-P59.3-EXTERNAL-ROOTS-001
phase=59.3
status=resolved
answer_identity=user message on 2026-08-24: `4つのパスを追加して続けて`
selected_option=authorize all required external owner repositories
authorized_next_state=PLAN
consumed=true

## Decision

The developer authorized the exact external update roots required for the
DOC-03 authoring, source-packaging, and public-projection toolchains:

- `/Users/asami/src/dev2025/cozy`
- `/Users/asami/src/dev2026/sbt-cozy`
- `/Users/asami/src/dev2025/smartdox`
- `/Users/asami/src/dev2025/simplemodeling-org`

The existing CNCF task root remains an update root. Its `ai/directive`
submodule is already a descendant of that root and needs no separate root
authorization.

## Frozen Scope Rule

Phase 59.3 may mutate only paths later named by a frozen implementation or
commit manifest in the admitted CNCF, Cozy, sbt-cozy, SmartDox, and
SimpleModeling.org projects. It must preserve unrelated current work in each
repository and must not broaden this authorization to another repository,
publication, deployment, or push.

## Recovery Evidence

At the decision boundary, the CNCF worktree was clean at
`e294c58245c25a9fd7b2929b5a45cef050f309bf`. Cozy, sbt-cozy, and
SimpleModeling.org contained unrelated uncommitted work; SmartDox and the
CNCF `ai/directive` submodule were clean. Those dirty paths are preserve paths,
not Phase 59.3 evidence or ownership.

## Authority and Resume

The accepted DOC-01A and DOC-01B registries assign DOC-03 acceptance groups to
Cozy, sbt-cozy, SmartDox, and SimpleModeling.org. This decision supplies the
previously missing update-root authority only. The parent must now rebuild the
Phase 59.3 plan, freeze the admitted repository set and preserve paths, and
perform the normal protected-boundary preflight before implementation.

## Writable-Root Resolution

decision_id=D-P593-SBT-COZY-WRITE-ROOT-001
status=authorized-pending-platform-application
answer_identity=user message on 2026-08-24: `/Users/asami/src/dev2026/sbt-cozy を追加して`
selected_option=add the already admitted sbt-cozy repository as this task's writable root
affected_identity=Phase 59.3 / DOC03-SOURCE-ARCHIVE-PROJECTION / sbt-cozy fixture repair
authorized_next_state=FIX
consumed=true

The developer selected the writable-root option for the exact sbt-cozy
repository. This is a continuation authorization only: it changes neither the
frozen repository set nor the planned source-archive contract. Actual editing
may resume only after the task environment exposes this exact path as writable.

# Phase 59.3 SmartDox Projection Acceptance

status=implementation-complete-awaiting-validation
date=2026-08-25
phase=[Phase 59.3](../../../../phase/phase-59.3.md)
slice=P593-DOC03-DOX-ACCEPTANCE-071

This record documents implementation completion for the SmartDox projection
acceptance slice. It does not claim acceptance or closure. Focused validation
and the mandatory independent lightweight Step review remain pending.

## Source Evidence

- SmartDox current HEAD: `4f0a46125ad20734f84e64446383c598e39fb35e`.
- Projection implementation commit: `a0bf1fd40617e7091f167254282cb0e8962159a1`.
- Predecessor: `8cb32ddb58216bb7ba6bdd68016db6729f89c7e1`.
- Implementation path:
  `src/main/scala/org/smartdox/service/operations/SmartDoxPublicationProjection.scala`.
- Executable specification path:
  `src/test/scala/org/smartdox/service/operations/SmartDoxPublicationProjectionSpec.scala`.

The predecessor lacked both exact acceptance paths above. The
`git diff --name-only 8cb32ddb58216bb7ba6bdd68016db6729f89c7e1 a0bf1fd --`
comparison proves that both paths first materialized in `a0bf1fd`. This is
attributable failing-first materialization evidence, not a fabricated test
failure; this record asserts no predecessor test failure.

## DOC-03 Acceptance Mapping

| Acceptance contract | Evidence-bearing implementation/specification behavior |
| --- | --- |
| `DOC03-DOX-AC-01` | Canonical `.dox`, `.md`, and `.markdown` inputs use the selected parsing profiles; preserved legacy `.adoc`, `.asciidoc`, and `.html` inputs are rejected. Canonical unresolved include directives are rejected before resolution. |
| `DOC03-DOX-AC-02` | Canonical projection produces deterministic HTML and records the LaTeX PDF renderer profile without invoking a renderer. |
| `DOC03-DOX-AC-03` | Projected and rejected outcomes retain the source name and lowercase UTF-8 SHA-256 digest, including canonical inputs containing unresolved includes. |

## Pending Closure Evidence

The only pending focused validation is:

`testOnly org.smartdox.service.operations.SmartDoxPublicationProjectionSpec`

The mandatory independent lightweight Step review is also pending. No
validation result or GREEN status is asserted here.

## Scope Boundary

This record includes no runtime renderer execution, publication, deployment,
resolver operation, source archive, Phase 58 modification, or edit to any
SmartDox foreign dirty path. The SmartDox implementation and executable
specification remain untouched by this journal change. No successor Phase,
changelog, checklist, or Phase-status change is included.

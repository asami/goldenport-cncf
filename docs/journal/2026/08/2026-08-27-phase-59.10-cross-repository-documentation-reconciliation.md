# Phase 59.10 DOC-10-B1 Cross-Repository Documentation Reconciliation

status = historical, evidence-bearing, non-normative
phase = Phase 59.10
slice = P5910-DOC10-B1

## Purpose and boundary

This record reconciles pre-existing, verified documentation and executable
evidence across the Core, Cozy, SimpleModeling.org, ai-directive, Textus CBD
Support, and Textus BoK repositories. It records the current canonical
responsibility boundaries for DOC-10. It is not Phase closure, does not start
Phase 60, and does not claim a new runtime, API, schema, resolver, or authority
implementation.

The Phase 59.10 checklist remains the governing progress record. The checklist
continues to report `status=planned` and DOC-10 `IN_PROGRESS` until the
remaining contradictory-current-document reconciliation, validation evidence,
strategy/dashboard updates, and final Phase closure are completed by the
parent workflow.

## Core evidence

Core commit `7c996b12eabf972aa040f2a86d7c02c49afb9a38` contains the accepted
canonical documentation package and its predecessor evidence:

- `docs/design/component-documentation-knowledge-package.md` — current design
  boundary;
- `docs/spec/component-documentation-knowledge-package.md` — paired normative
  specification;
- `docs/notes/component-documentation-knowledge-package-implementation.md` —
  historical, superseded implementation note; and
- `docs/phase/phase-59.10-checklist.md` — the checklist predecessor state
  carried into this reconciliation.

These Core records establish that documentation is a read-only projection of
resolved Component resources. They do not create a second identity model,
carrier, route, resolver, runtime behavior, schema, or authority boundary.

## Cross-repository canonical evidence

### Cozy

Cozy commit `c962b1c`, `docs/design/car-documentation-lint.md`, defines
author-owned manual inputs and deterministic documentation lint. Cozy commit
`beacea8`, `docs/design/component-repository-publication.md`, defines
repository-publication discovery and consistency mechanics. These documents
remain bounded to author-owned inputs, documentation checks, and publication
index discovery; Cozy is not the Phase 58 resolver or the public framework
authoring authority, and these documents do not expand runtime authority.

### SimpleModeling.org

SimpleModeling.org commit `12ae35150ca5305d2a3cb52fc5ee4bcca1b776f9`
contains:

- `src/main/publication/cncf-framework-publication.json`; and
- `scripts/test/check-cncf-framework-publication-contract.sh`.

The publication record uses the exact `cncf.framework-publication.v1`
contract. Authored source is authoritative for the publication. Versioned
locale HTML and structured machine-readable projections are publication
outputs; a publication URL, digest, snapshot, or metadata record does not
become Component identity, runtime, resolver, or Directive authority.
SimpleModeling.org has a dedicated static publication-contract script. This
record does not claim that script was executed; parent validation occurs after
this Slice.

### ai-directive

ai-directive commit `d18af546d1fdce7696d89d6c3ecdbc33d111a9dc` contains:

- `public/ai-development-guide.md`;
- `scripts/test/check-cncf-public-directive-projection.sh`; and
- the README core/profile authority guidance.

The public development guide is descriptive. The mounted `core/` and selected
profile remain authoritative, and samples or local extensions cannot override
that authority. Public metadata is nonactivating: it does not install,
activate, execute, configure, disclose, or grant authority to a Skill or MCP
requirement. Actual `SkillBundleManifest` packaging, installation, and
activation remain deferred outside this Slice. ai-directive has a dedicated
static public-Directive projection script. This record does not claim that
script was executed; parent validation occurs after this Slice.

### Textus CBD Support

Textus CBD Support commit `0f707eafdda5cd9a9d2d1f6ca924d7aad051b6ed`
contains:

- `docs/design/component-knowledge-carrier-integration.md`; and
- `docs/spec/component-knowledge-carrier-consumer.md`.

CBD Support consumes declared carrier paths and digests after exact selection
of the Component and release. Its bounded, read-only detail, usage, MCP, and
CAR Review projections expose declared metadata and safe logical identities,
not raw bodies, physical paths, resolver output, executable operations, or
execution authority. CBD Support owns component detail, usage, and review
projections from declared carrier evidence; it does not gain a new resolver or
operation authority.

### Textus BoK

Textus BoK commit `541c9f7fbca8cb2100ca5fde1647cf54bc37f1f7` contains:

- `docs/design/bok-semantic-retrieval.md`; and
- `docs/spec/bok-semantic-retrieval-contract.md`.

BoK provides complementary semantic and terminology evidence by projecting
declared semantic fields only. It does not expose resource bodies,
installation or activation behavior, or Directive override authority. BoK
provides an exact existence handoff to CBD Support for independently selected
component detail and usage; BoK does not produce that detail or usage itself.

## Evidence state and deferred work

The SimpleModeling.org and ai-directive static contract scripts named above
are existing executable evidence sources, not execution receipts in this
historical record. Their execution, Core static checks, diff verification,
independent review, and final Phase closure remain pending under the parent
workflow. No executable specification is created or modified by this record.

Actual `SkillBundleManifest` packaging, installation, and activation remains
deferred to its separately governed work. Contradictory current-document
reconciliation and strategy/dashboard closure remain pending in the checklist.
